apiVersion: v1
kind: Service
metadata:
  name: {{ include "ct-iot-thing-service.fullname" . }}
  labels:
    {{- include "ct-iot-thing-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ct-iot-thing-service.selectorLabels" . | nindent 4 }}
