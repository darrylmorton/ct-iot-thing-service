apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ct-iot-thing-service.fullname" . }}-test-connection"
  labels:
    {{- include "ct-iot-thing-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ct-iot-thing-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
